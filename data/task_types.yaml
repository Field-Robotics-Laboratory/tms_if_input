# ver3.xml (ConstructionPlan/scxml) の <state name="..."> と
# 生成するBehaviorTree XMLの <Action ID="..."> の対応表。
# 未知のstate名が現れた場合、taskset_compilerはこの表に無いことを警告ログに出し、
# state名をそのままAction IDとして使う。
task_types:
  ExcavationLoading: ExcavationLoading
  Transport: Transport
  Leveling: Leveling
